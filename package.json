{
  "name": "taper-monorepo",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/schmug/taper.git"
  },
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck && tsc -p scripts --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "probe": "node scripts/probe-claude-code.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/node": "^22.18.0",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
