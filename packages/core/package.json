{
  "name": "@taper/core",
  "version": "0.0.0",
  "description": "Pure decay engine: state machine, guards, clocks, evaluate/explain/simulate. No I/O.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/schmug/taper.git",
    "directory": "packages/core"
  },
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit && tsc -p test --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "5.0.1",
    "fast-check": "4.10.2"
  }
}
